import base64


def base64_encode(text):
    return base64.b64encode(text.encode()).decode()


tekst = "I wonder if this task is done correctly."
tekst_base64 = base64_encode(tekst)
print("Zaszyfrowany tekst:", tekst_base64)
